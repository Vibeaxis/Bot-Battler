// We are keeping the component definition so the app doesn't crash,
// but we removed the logic so it stops messing with your scroll position.

const ScrollToTop = () => {
    return null;
}

export default ScrollToTop;